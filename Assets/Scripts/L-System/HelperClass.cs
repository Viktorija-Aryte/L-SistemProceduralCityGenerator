using UnityEngine;

/*
 * Saves/load the Rotation and Position when we have the '['']' 
 */
public class HelperClass
{
    public Vector3 position, direction;

    //this gets update in the process
    public int stepSize;
    
    
}
